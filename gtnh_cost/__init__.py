"""GTNH raw-material cost model -- the pure model package.

No web imports live below this package; the browser layer in `app/` is a thin
view over it, which is what lets auto mode attach later without touching the UI.
"""
